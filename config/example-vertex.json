{
  "provider": "vertex",

  "project_id": "my-project",
  "location": "global",

  "model": {
    "default": "nano-banana-pro"
  },

  "output": {
    "directory": "./generated"
  },

  "credentials": {
    "source": "repo"
  },

  "auth": {
    "type": "service_account",
    "credentials_file": "./gcp-service-account.json"
  },

  "_comment": "Swap the auth block for {\"type\":\"adc\"} to use application default credentials, or drop it and set an api_key for Vertex express mode (which ignores project_id and location)."
}
