{
  "provider": "gemini",

  "model": {
    "default": "nano-banana-2"
  },

  "output": {
    "directory": "./generated",
    "overwrite": false
  },

  "credentials": {
    "source": "auto"
  },

  "api_style": "interactions",
  "api_version": "v1beta",
  "http_timeout": 180,
  "log_level": "info",

  "_comment": "Put api_key here, or leave it out and use GEMINI_API_KEY. For Vertex, set provider to vertex and add project_id and location. See config/example-vertex.json."
}
